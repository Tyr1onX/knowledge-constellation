# External Audit comparison — karpathy 01

## Verdict

**PASS_WITH_WARNINGS**

Recognition and attribution are strong: the Runner does not credit autoresearch agent edits or discoveries as direct human implementation, does not convert dependency use into library authorship, and preserves substantial first-principles training/CUDA/teaching evidence.

The material warning is structural. `A3 / G3` combines `神经网络第一性教学与解释` and `Agent 驱动研究流程设计` under a synthetic umbrella (`Executable neural-network understanding and agent research`). The frozen sources do not describe those as one recurring activity stream; they are two real but separate experiences.

This appears to be partly induced by the current Structure contract requiring every Galaxy to have at least two primary nodes. That hard minimum can force a sparse or isolated but representative theme to borrow an unrelated companion node.

## Generalized fix

Allow a one-node Galaxy when the evidence genuinely supports one isolated representative theme and no second node is justified. The Skill should prefer a small truthful Galaxy over fabricating or merging semantics to satisfy layout shape. Multi-node Galaxies remain preferred when real structure exists.

After the contract change, rerun this frozen target without changing its sources.
