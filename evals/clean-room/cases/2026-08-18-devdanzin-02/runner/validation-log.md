# Validation log — devdanzin regression 02

Runner configuration: `2f0e22dc1d134336f56d74818b648b7f7438dc6e`
Isolation: `emulated`
Frozen source scope: identical to case `2026-08-18-devdanzin-01`

## Contract preflight

- Pass A reused the previously accepted Evidence because the frozen source scope and Pass-A contract were unchanged.
- Pass B claim subjects resolve to accepted node ids; every node preserves known/unknown/boundary and evidence provenance.
- Mixed attribution is no longer hidden in the node label: `崩溃复现与验证` excludes AI-assisted reducer/minimizer authorship.
- Pass C contains no `trajectory` relation, because the frozen sources do not establish a chronological migration.
- Cross-cutting AI-assisted workflow and ecosystem reliability are represented as `motifs`, not forced Galaxies.
- Every accepted node belongs to exactly one Galaxy and exactly one display layer.
- Pass D includes every accepted Structure Anchor exactly once and derives its Galaxy membership from accepted node ownership.
- `identity.label` is the frozen input subject label `devdanzin`.

No repair was required in this emulated rerun.

> Environment note: this connector-only run did not execute the repository's local Python harness subprocess. The outputs were checked against the current schemas and semantic invariants before freeze; repository CI remains the executable contract check for code/schema changes.
